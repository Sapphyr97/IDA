{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ product.Titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.Description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ product.Prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.Image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_ad_index') }}">back to list</a>

    <a href="{{ path('app_product_ad_edit', {'id': product.id}) }}">edit</a>

    {{ include('product_ad/_delete_form.html.twig') }}
{% endblock %}
