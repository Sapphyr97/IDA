{% extends 'base.html.twig' %}

{% block title %}Les Formations{% endblock %}

{% block body %}

 {% for product in products %}
<div class="bg-light p-3">
		<h1>{{ product.titre }}</h1>
	</div>

	<div class="row">
		<div class="col-12 mt-4">
			<div class="border border-dark rounded p-2 clearfix">
				<div class="d-flex">
					<img class="img-fluid rounded col-6" src="{{product.image}}" alt="Image de {{ product.titre }}">
					<div class="row">
						<div class="col-12">
							<h2 class="text-right">
								{{ product.prix }}
								€
							</h2>
							<hr>
							<p>{{ product.description }}</p>
							<hr>
						</div>
					</div>
				</div>
				<a href="{{ path('panier_add', {'id': product.id }) }}" class="btn btn-success float-right">
					<i class="fas fa-shopping-cart mr-1"></i>
					Ajouter
				</a>
			</div>
		</div>
	</div>
     {% endfor %}
{% endblock %}
{# src="assets\images\1.jpg" {{ product.image }} #}